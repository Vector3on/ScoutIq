/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This line tells Next.js to export the site as static files.
  output: 'export',
};

module.exports = nextConfig;
